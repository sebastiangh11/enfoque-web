---
import Container from "./Container.astro";

const whatsappBaseUrl = "https://wa.me/525574570826";
---

<section id="contacto" class="relative overflow-hidden border-t border-border py-16 md:py-20">
  <div
    class="pointer-events-none absolute inset-0 opacity-90 [background:radial-gradient(circle_at_top,color-mix(in_srgb,var(--brand-blue)_8%,transparent),transparent_55%),radial-gradient(circle_at_20%_15%,color-mix(in_srgb,var(--brand-orange)_10%,transparent),transparent_45%)]"
    aria-hidden="true"
  ></div>

  <Container class="relative">
    <div class="grid gap-12 lg:grid-cols-[1.05fr_0.95fr] lg:items-start">
      <div class="space-y-6">
        <div class="space-y-3">
          <p class="text-xs font-semibold uppercase tracking-[0.32em] text-brand-blue">CONVERSIÓN</p>
          <h2 class="text-3xl font-bold text-text-primary md:text-4xl">
            Producción de branding físico para empresas en México.
          </h2>
          <p class="text-text-secondary">
            Atendemos corporativos, medianas empresas, PYMEs y retailers. Nos aventamos cualquier proyecto y entregamos
            propuestas claras para arrancar rápido.
          </p>
        </div>

        <ul class="grid gap-3 text-sm text-text-secondary sm:grid-cols-2">
          <li class="flex items-center gap-3">
            <span class="flex h-9 w-9 items-center justify-center rounded-full border border-border bg-surface-soft text-brand-blue">
              <svg class="h-4 w-4" fill="none" stroke="currentColor" viewBox="0 0 24 24" aria-hidden="true">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 7h18M3 12h18M3 17h18" />
              </svg>
            </span>
            <span>Atendemos corporativos y PYMEs</span>
          </li>
          <li class="flex items-center gap-3">
            <span class="flex h-9 w-9 items-center justify-center rounded-full border border-border bg-surface-soft text-brand-blue">
              <svg class="h-4 w-4" fill="none" stroke="currentColor" viewBox="0 0 24 24" aria-hidden="true">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v8m4-4H8" />
              </svg>
            </span>
            <span>Producción rápida</span>
          </li>
          <li class="flex items-center gap-3">
            <span class="flex h-9 w-9 items-center justify-center rounded-full border border-border bg-surface-soft text-brand-blue">
              <svg class="h-4 w-4" fill="none" stroke="currentColor" viewBox="0 0 24 24" aria-hidden="true">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7" />
              </svg>
            </span>
            <span>Calidad premium</span>
          </li>
          <li class="flex items-center gap-3">
            <span class="flex h-9 w-9 items-center justify-center rounded-full border border-border bg-surface-soft text-brand-blue">
              <svg class="h-4 w-4" fill="none" stroke="currentColor" viewBox="0 0 24 24" aria-hidden="true">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 6v12m6-6H6" />
              </svg>
            </span>
            <span>Nos adaptamos a cualquier proyecto</span>
          </li>
        </ul>

        <div class="rounded-2xl border border-border bg-surface p-6 shadow-sm">
          <p class="text-sm font-semibold text-text-primary">Contacto directo</p>
          <div class="mt-4 flex flex-wrap gap-3 text-sm">
            <a
              href="mailto:ventas@segac.com.mx"
              class="inline-flex items-center gap-2 rounded-full border border-border px-4 py-2 font-semibold text-text-secondary transition hover:border-brand-blue hover:text-brand-blue"
            >
              ventas@segac.com.mx
            </a>
            <a
              href={whatsappBaseUrl}
              target="_blank"
              rel="noopener noreferrer"
              class="inline-flex items-center gap-2 rounded-full border border-border px-4 py-2 font-semibold text-text-secondary transition hover:border-brand-blue hover:text-brand-blue"
            >
              WhatsApp +52 55 7457 0826
            </a>
          </div>
        </div>
      </div>

      <div class="rounded-2xl border border-border bg-surface p-6 shadow-sm md:p-8">
        <p class="text-sm font-semibold uppercase tracking-[0.2em] text-text-secondary">
          Cuéntanos de tu proyecto y te preparamos una propuesta.
        </p>

        <form class="mt-6 grid gap-4" aria-label="Formulario de cotización" data-contact-form>
          <label class="grid gap-2 text-sm font-semibold text-text-primary">
            Nombre
            <input
              type="text"
              name="nombre"
              required
              placeholder="Tu nombre"
              class="w-full rounded-xl border border-border bg-surface-soft px-4 py-3 text-sm text-text-primary placeholder:text-text-muted focus:outline-none focus:ring-2 focus:ring-brand-orange"
            />
          </label>

          <label class="grid gap-2 text-sm font-semibold text-text-primary">
            Empresa
            <input
              type="text"
              name="empresa"
              required
              placeholder="Empresa"
              class="w-full rounded-xl border border-border bg-surface-soft px-4 py-3 text-sm text-text-primary placeholder:text-text-muted focus:outline-none focus:ring-2 focus:ring-brand-orange"
            />
          </label>

          <label class="grid gap-2 text-sm font-semibold text-text-primary">
            Teléfono / WhatsApp
            <input
              type="tel"
              name="telefono"
              required
              placeholder="55 0000 0000"
              class="w-full rounded-xl border border-border bg-surface-soft px-4 py-3 text-sm text-text-primary placeholder:text-text-muted focus:outline-none focus:ring-2 focus:ring-brand-orange"
            />
          </label>

          <label class="grid gap-2 text-sm font-semibold text-text-primary">
            Email
            <input
              type="email"
              name="email"
              required
              placeholder="tu@empresa.com"
              class="w-full rounded-xl border border-border bg-surface-soft px-4 py-3 text-sm text-text-primary placeholder:text-text-muted focus:outline-none focus:ring-2 focus:ring-brand-orange"
            />
          </label>

          <label class="grid gap-2 text-sm font-semibold text-text-primary">
            Ciudad / Estado (opcional)
            <input
              type="text"
              name="ciudad"
              placeholder="CDMX, Guadalajara, Monterrey"
              class="w-full rounded-xl border border-border bg-surface-soft px-4 py-3 text-sm text-text-primary placeholder:text-text-muted focus:outline-none focus:ring-2 focus:ring-brand-orange"
            />
          </label>

          <label class="grid gap-2 text-sm font-semibold text-text-primary">
            Tipo de proyecto
            <select
              name="tipoProyecto"
              required
              class="w-full rounded-xl border border-border bg-surface-soft px-4 py-3 text-sm text-text-primary focus:outline-none focus:ring-2 focus:ring-brand-orange"
            >
              <option value="" disabled selected>Selecciona una opción</option>
              <option>Branding para oficinas</option>
              <option>Retail y punto de venta</option>
              <option>Eventos y activaciones</option>
              <option>Señalización corporativa</option>
              <option>Proyecto especial</option>
            </select>
          </label>

          <label class="grid gap-2 text-sm font-semibold text-text-primary">
            Mensaje
            <textarea
              name="mensaje"
              required
              rows={4}
              placeholder="Cuéntanos lo que necesitas"
              class="w-full resize-none rounded-xl border border-border bg-surface-soft px-4 py-3 text-sm text-text-primary placeholder:text-text-muted focus:outline-none focus:ring-2 focus:ring-brand-orange"
            ></textarea>
          </label>

          <div class="mt-2 grid gap-3 sm:grid-cols-2">
            <button
              type="submit"
              class="inline-flex w-full items-center justify-center rounded-full bg-brand-orange px-6 py-4 text-sm font-semibold text-white transition hover:bg-brand-orange/90 focus:outline-none focus:ring-2 focus:ring-brand-orange focus:ring-offset-2"
            >
              Enviar solicitud de cotización
            </button>
            <a
              href={whatsappBaseUrl}
              target="_blank"
              rel="noopener noreferrer"
              data-whatsapp-link
              class="inline-flex w-full items-center justify-center rounded-full border-2 border-brand-blue px-6 py-4 text-sm font-semibold text-brand-blue transition hover:bg-brand-blue/10 focus:outline-none focus:ring-2 focus:ring-brand-orange focus:ring-offset-2"
            >
              Cotizar por WhatsApp
            </a>
          </div>

          <p class="text-sm text-text-secondary">Respuesta en menos de 24 hrs hábiles.</p>
        </form>
      </div>
    </div>
  </Container>
</section>

<script>
  const form = document.querySelector("[data-contact-form]");
  const whatsappLink = document.querySelector("[data-whatsapp-link]");

  const getFieldValue = (name) => {
    if (!form) return "";
    const field = form.querySelector(`[name="${name}"]`);
    return field?.value?.trim() ?? "";
  };

  const buildWhatsappMessage = () => {
    const nombre = getFieldValue("nombre");
    const empresa = getFieldValue("empresa");
    const tipoProyecto = getFieldValue("tipoProyecto");
    const ciudad = getFieldValue("ciudad");
    const mensaje = getFieldValue("mensaje");

    return [
      "Hola, me gustaría cotizar un proyecto.",
      `Nombre: ${nombre || "-"}`,
      `Empresa: ${empresa || "-"}`,
      `Tipo de proyecto: ${tipoProyecto || "-"}`,
      `Ciudad/Estado: ${ciudad || "-"}`,
      `Mensaje: ${mensaje || "-"}`,
    ].join("\n");
  };

  if (whatsappLink) {
    whatsappLink.addEventListener("click", (event) => {
      const message = buildWhatsappMessage();
      const url = `https://wa.me/525574570826?text=${encodeURIComponent(message)}`;
      whatsappLink.setAttribute("href", url);
      window.open(url, "_blank", "noopener,noreferrer");
      event.preventDefault();
    });
  }

  if (form) {
    form.addEventListener("submit", (event) => {
      event.preventDefault();

      const nombre = getFieldValue("nombre");
      const empresa = getFieldValue("empresa") || "Empresa";
      const telefono = getFieldValue("telefono");
      const email = getFieldValue("email");
      const ciudad = getFieldValue("ciudad");
      const tipoProyecto = getFieldValue("tipoProyecto");
      const mensaje = getFieldValue("mensaje");

      const subject = `Solicitud de cotización — ${empresa}`;
      const body = [
        "Hola, me gustaría recibir una cotización.",
        "",
        `Nombre: ${nombre}`,
        `Empresa: ${empresa}`,
        `Teléfono/WhatsApp: ${telefono}`,
        `Email: ${email}`,
        `Ciudad/Estado: ${ciudad || "-"}`,
        `Tipo de proyecto: ${tipoProyecto}`,
        `Mensaje: ${mensaje}`,
      ].join("\n");

      const mailtoUrl = `mailto:ventas@segac.com.mx?subject=${encodeURIComponent(subject)}&body=${encodeURIComponent(body)}`;
      window.location.href = mailtoUrl;
    });
  }
</script>
